/*	1.变量名要以字母开头
  	2.可以用于变量名的字符有 大小写字母、数字、下划线
	3.建议不要用下划线开头，因为可能会与头文件某些变量定义冲突
	4.作为规范，变量的首字母请不要大写
	5.已有关键字不能做变量名，已有函数名也不要做变量名 
	
	正确变量名:			错误变量名: 
	cat					#money
	cat2				2cat
	max_value			max-value
	_abd				do not
	taxRate				don't
	
	
	关键字:
	auto		enum		restrict	unsigned
	break		extern		return		void
	case		float		short		volatile
	char		for			signed		while
	const		goto		sizeof		_Bool
	continue	if			static		_Complex
	default		inline		struct		_Imaginary
	do			int			switch
	double		long		typedef
	else		register	union
*/ 
	
	
	 
 
