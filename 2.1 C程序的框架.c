//这一行是注释，编译器会忽略//后面所有的内容直至这一行结束

/*
	在这范围内都属于注释，从上面那两个字符开始，直到遇到下面那两个字符结束
*/ 


/*
    int指函数的返回值类型是整数，main()是指主函数，每个程序都只有一个main函数，
是程序运行的入口，由系统来调用main，所以我们不能自己调用自己。
*/	


int main(void)	//void指空，无类型。在括号内可以省略
{
	return 0;	//返回0给系统，代表程序正常终止，这句话不要省略
}



//一个控制台程序的框架必须包含上述代码 
